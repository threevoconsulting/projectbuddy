# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Local data — never commit a household's database
*.db
*.db-wal
*.db-shm
buddy.db*

# Env / secrets
.env

# Local TLS certs (generated by scripts/make_cert.sh; never commit private keys)
/certs/

# Downloaded model weights (fetched on the Mac, never committed). Anchored to the
# repo root so it does NOT match the source package src/projectbuddy/models/.
/models/
*.onnx
*.gguf

# OS / editor
.DS_Store
.idea/
.vscode/
